@{
    ViewData["Title"] = "تنظیمات حساب کاربری";
}

<div class="container py-4">
    <div class="row">
        <div class="col-md-3">
            <!-- منوی تنظیمات -->
            <div class="card shadow-sm">
                <div class="list-group list-group-flush">
                    <a asp-area="Identity" asp-page="/Account/Manage/Index"
                       class="list-group-item list-group-item-action">
                        <i class="bi bi-person"></i> اطلاعات حساب
                    </a>
                    <a asp-area="Identity" asp-page="/Account/Manage/Email"
                       class="list-group-item list-group-item-action">
                        <i class="bi bi-envelope"></i> ایمیل
                    </a>
                    <a asp-area="Identity" asp-page="/Account/Manage/ChangePassword"
                       class="list-group-item list-group-item-action">
                        <i class="bi bi-key"></i> تغییر رمز عبور
                    </a>
                    <a asp-area="Identity" asp-page="/Account/Manage/PersonalData"
                       class="list-group-item list-group-item-action">
                        <i class="bi bi-download"></i> دریافت اطلاعات
                    </a>
                </div>
            </div>
        </div>

        <div class="col-md-9">
            <!-- محتوای تنظیمات -->
            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h5 class="mb-0">
                        <i class="bi bi-gear"></i> @ViewData["Title"]
                    </h5>
                </div>
                <div class="card-body">
                    <p class="text-muted">
                        برای تغییر تنظیمات حساب کاربری، از منوی سمت راست گزینه مورد نظر را انتخاب کنید.
                    </p>

                    <div class="alert alert-info">
                        <i class="bi bi-info-circle"></i>
                        <strong>توجه:</strong> تنظیمات امنیتی و شخصی‌سازی حساب از طریق Identity مدیریت می‌شود.
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>